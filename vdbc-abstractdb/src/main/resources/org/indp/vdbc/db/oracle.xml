<dialect extends="generic" name="Oracle">
    <example-urls>
        <url>jdbc:oracle:thin:@hostname:port:sid</url>
    </example-urls>
    <drivers>
        <driver>oracle.jdbc.OracleDriver</driver>
    </drivers>
    <features>
        <feature id="select.all.from.table.limit.offset"
                 expression="select * from (select t.*, rownum r$vdbc$ignore from (select * from @{tableName}) t) where r$vdbc$ignore between ${offset+1} and @{offset+limit}"/>
    </features>
</dialect>